[package]
name = "dhcpv6"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
license = "MIT"
authors = ["zevs"]
description = "Minimal no-panic DHCPv6 (RFC 8415) message + option codec: encode/parse client-server messages and options. Zero dependencies."
keywords = ["dhcpv6", "rfc8415", "codec", "ipv6", "network"]
categories = ["network-programming", "parser-implementations"]
readme = "README.md"
repository = "https://github.com/icedracon/dhcpv6"

[dependencies]
# none — pure std codec, no I/O and no agent logic.
